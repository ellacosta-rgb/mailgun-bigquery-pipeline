# mailgun-bigquery-pipeline
Build a daily pipeline that pulls the previous day's data from the Mailgun Analytics Logs API and loads it into a BigQuery table for use in our dashboard. The script will be deployed as a Cloud Run function on a daily schedule.
